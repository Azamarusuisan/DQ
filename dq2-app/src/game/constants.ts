/** Tile size in pixels (internal resolution). */
export const T = 16;

/** Screen width in pixels (internal resolution: 256px). */
export const SW = 256;

/** Screen height in pixels (internal resolution: 240px). */
export const SH = 240;

/** Number of tile columns visible on screen. */
export const COLS = SW / T; // 16

/** Number of tile rows visible on screen. */
export const ROWS = SH / T; // 15
